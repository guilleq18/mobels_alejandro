# ⚡ QUICK FIX CARD - Git Not in Directory Error

**Error:**
```
fatal: not in a git directory
```

**Cause:** Repository not cloned yet

**Solution (Copy & Paste):**

```bash
# Step 1: Make sure you're in the right place
cd ~/domains/mobelsalejandro.shop/public_html

# Step 2: Clone the repository
git clone https://github.com/guilleq18/mobels_alejandro.git .

# Step 3: Configure git
git config user.email "alejandro.willi@gmail.com"
git config user.name "Alejandro"

# Step 4: Verify
git status
```

**Expected Result:**
```
On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
```

✅ **Done!** Now continue with deployment.

---

## Or Run All at Once:

```bash
cd ~/domains/mobelsalejandro.shop/public_html && git clone https://github.com/guilleq18/mobels_alejandro.git . && git config user.email "alejandro.willi@gmail.com" && git config user.name "Alejandro" && git status
```

---

**Next:** Follow [HOSTINGER_MANUAL_DEPLOYMENT.md](HOSTINGER_MANUAL_DEPLOYMENT.md) starting at "STEP 6: Install PHP Dependencies"
