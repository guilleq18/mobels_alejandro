🎯 INSTALACIÓN FINAL - PASO A PASO DEFINITIVO

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ESTADO CONFIRMADO:
✓ Home directory: /home/u519347385/
✓ Carpeta domains existe
✓ SSH conectado correctamente
✓ Estructura: ~/domains/mobelsalejandro.shop/public_html/laravel_app/

PRÓXIMO: Verificar dominio y completar instalación

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PASO 1: VERIFICAR QUE EXISTEN CARPETAS

Ejecuta ESTOS dos comandos:

ls -la ~/domains/

Reporta qué carpetas ve (debe estar mobelsalejandro.shop)


PASO 2: SI EXISTE mobelsalejandro.shop, VER CONTENIDO

ls -la ~/domains/mobelsalejandro.shop/public_html/


PASO 3: VER CONTENIDO DE laravel_app

ls -la ~/domains/mobelsalejandro.shop/public_html/laravel_app/


PASO 4: Si TODO EXISTE, EJECUTAR ESTO (COPIAR Y PEGAR):

cd ~/domains/mobelsalejandro.shop/public_html/laravel_app

PASO 5: INSTALAR COMPOSER (si tarda, espera pacientemente - 10-15 min)

composer install --no-dev --optimize-autoloader

PASO 6: CONFIGURAR GIT

git config user.email "alejandro.willi@gmail.com"
git config user.name "Alejandro"

PASO 7: CREAR .env

cp .env.example .env

PASO 8: EDITAR .env - IMPORTANTE

nano .env

Dentro de nano, busca y edita ESTAS líneas:

DB_HOST=localhost
DB_DATABASE=u519347385_mobels
DB_USERNAME=u519347385_mbaguero
DB_PASSWORD=Alejandro123!

APP_URL=https://mobelsalejandro.shop
APP_KEY=base64:KVWaKh6F7sKlqd9zxL0p2mN3jKvBqY5rU8tW9xP0qY1=

PARA SALVAR EN NANO:
Presiona: Ctrl+O (letra O)
Presiona: ENTER
Presiona: Ctrl+X

PASO 9: EJECUTAR MIGRACIONES

php artisan migrate --force

PASO 10: CREAR USUARIO ADMIN

php artisan db:seed

PASO 11: VERIFICAR QUE FUNCIONÓ

php artisan tinker

Adentro, escribe:

User::all();

Debe mostrar el usuario creado

Luego: exit

PASO 12: SALIR DE SSH

exit

PASO 13: PROBAR EN NAVEGADOR

Abre: https://mobelsalejandro.shop/admin/login

Ingresa:
Email: alejandro@example.com
Password: password

¿Entraste? ✓ ¡LISTO! TODO FUNCIONA

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SI FALLA EN ALGÚN PASO:

❌ "No such file or directory"
→ La carpeta no existe
→ Ejecuta: ls -la ~/domains/mobelsalejandro.shop/public_html/

❌ "composer: command not found"
→ Instalar composer:
curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer

❌ "SQLSTATE[HY000]: General error"
→ Error de BD o .env
→ Verifica: cat .env | grep DB_

❌ "Could not open input file: artisan"
→ No estás en la carpeta correcta
→ Ejecuta: pwd
→ Debe mostrar: /home/u519347385/domains/mobelsalejandro.shop/public_html/laravel_app

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

COMANDOS PARA COPIAR/PEGAR COMPLETOS:

TODO EN UNO (ejecutar línea por línea):

cd ~/domains/mobelsalejandro.shop/public_html/laravel_app
composer install --no-dev --optimize-autoloader
git config user.email "alejandro.willi@gmail.com"
git config user.name "Alejandro"
cp .env.example .env
nano .env

(edita .env con Ctrl+O, ENTER, Ctrl+X)

php artisan migrate --force
php artisan db:seed
php artisan tinker

(adentro: User::all(); luego exit)

exit

VERIFICAR ESTRUCTURA:

pwd
ls -la

LIMPIAR CACHE SI FALLA ALGO:

php artisan cache:clear
php artisan config:cache
php artisan route:cache
php artisan view:cache

VER LOGS SI FALLA:

cat ~/domains/mobelsalejandro.shop/public_html/laravel_app/storage/logs/laravel.log

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

DESPUÉS DE COMPLETAR:

Tu e-commerce MOBELS ALEJANDRO está 100% funcional:

✅ Sitio principal: https://mobelsalejandro.shop
✅ Panel admin: https://mobelsalejandro.shop/admin/login
✅ BD MySQL conectada
✅ Usuario admin creado
✅ Deployment automático desde GitHub activo

CAMBIOS FUTUROS (desde tu PC local):

git add .
git commit -m "Descripción de cambio"
git push origin master

(Se despliega automáticamente a Hostinger)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

¿LISTO? ¡Comienza ejecutando los comandos del PASO 1!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
